---
title: "API Reference"
description: "Complete reference for JARFIS slash commands, .jarfis-state.json schema, learning file structure, and phase specifications."
category: "api-reference"
order: 1
locale: "en"
lastUpdated: 2026-03-15
draft: false
---

# API Reference

Complete reference for all JARFIS commands, state schema, and configuration files.

## Slash Commands

<CommandCard command={{
  name: "/jarfis:work",
  description: "The primary command to start a JARFIS workflow. Initiates the full phase pipeline from Triage through Retrospective.",
  syntax: [
    "/jarfis:work [description]",
    "/jarfis:work [description] --meeting [meeting-name]"
  ],
  params: [
    { name: "description", required: true, description: "Natural language description of the feature or task to implement" },
    { name: "--meeting", required: false, description: "Name of a previous /jarfis:meeting session to use as input context" }
  ],
  examples: [
    "/jarfis:work Build a user authentication system with JWT tokens",
    "/jarfis:work Add Stripe subscription billing --meeting billing-kickoff",
    "/jarfis:work Fix the broken cart calculation on checkout page"
  ]
}} />

<CommandCard command={{
  name: "/jarfis:meeting",
  description: "Initiates a structured kickoff meeting for planning and alignment. Meeting results are stored and can be referenced by subsequent /jarfis:work sessions.",
  syntax: ["/jarfis:meeting [topic]"],
  params: [
    { name: "topic", required: true, description: "The subject or agenda for the meeting" }
  ],
  examples: [
    "/jarfis:meeting Define the authentication strategy for the mobile app",
    "/jarfis:meeting Decide on database schema for multi-tenant architecture"
  ]
}} />

<CommandCard command={{
  name: "/jarfis:continue",
  description: "Continues a completed workflow with a follow-up task. Reuses the existing branch and artifacts from the previous workflow for efficient follow-up work.",
  syntax: [
    "/jarfis:continue [description]",
    "/jarfis:continue [description] --workflow [path]",
    "/jarfis:continue [description] --mode fix|extend"
  ],
  params: [
    { name: "description", required: true, description: "Natural language description of the follow-up task" },
    { name: "--workflow", required: false, description: "Directly specify the workflow directory path" },
    { name: "--mode", required: false, description: "Explicitly specify mode: fix (bug fix) or extend (feature extension)" }
  ],
  examples: [
    "/jarfis:continue Add unit tests for the authentication module",
    "/jarfis:continue Fix the edge case in cart validation --mode fix",
    "/jarfis:continue Add OAuth support --workflow .jarfis/works/2026-03-10/auth-system"
  ]
}} />

<CommandCard command={{
  name: "/jarfis:implement",
  description: "Modifies or adds to the JARFIS system itself — commands, agents, and internal structure. The dedicated command for JARFIS self-improvement.",
  syntax: ["/jarfis:implement [description]"],
  params: [
    { name: "description", required: true, description: "Description of the JARFIS system modification or addition" }
  ],
  examples: [
    "/jarfis:implement Add a new /jarfis:standup command for daily status reports",
    "/jarfis:implement Modify the QA agent to include performance testing",
    "/jarfis:implement Update the Retrospective phase to output a structured JSON summary"
  ]
}} />

<CommandCard command={{
  name: "/jarfis:project-init",
  description: "Generates a project profile by analyzing the current codebase. Creates .jarfis/project-profile.md with information about the project structure, dependencies, and conventions.",
  syntax: ["/jarfis:project-init"],
  params: [],
  examples: ["/jarfis:project-init"]
}} />

<CommandCard command={{
  name: "/jarfis:project-update",
  description: "Updates an existing project profile to reflect changes in the codebase since the last initialization.",
  syntax: ["/jarfis:project-update"],
  params: [],
  examples: ["/jarfis:project-update"]
}} />

<CommandCard command={{
  name: "/jarfis:health",
  description: "Diagnoses zombie processes and other operational issues in the JARFIS environment.",
  syntax: ["/jarfis:health"],
  params: [],
  examples: ["/jarfis:health"]
}} />

<CommandCard command={{
  name: "/jarfis:upgrade",
  description: "Manages learning items — review, edit, and curate entries in the learning system.",
  syntax: ["/jarfis:upgrade"],
  params: [],
  examples: ["/jarfis:upgrade"]
}} />

<CommandCard command={{
  name: "/jarfis:version",
  description: "Displays and manages JARFIS version information.",
  syntax: ["/jarfis:version"],
  params: [],
  examples: ["/jarfis:version"]
}} />

<CommandCard command={{
  name: "/jarfis:distill",
  description: "Performs prompt distillation — optimizes and compresses prompt content for efficiency.",
  syntax: ["/jarfis:distill"],
  params: [],
  examples: ["/jarfis:distill"]
}} />

---

## State File: `.jarfis-state.json`

The state file tracks the current workflow progress. Located at the project root.

### Schema

```json
{
  "work_name": "string",
  "docs_dir": "string (path to artifact directory)",
  "branch": "string (Git branch name)",
  "base_branch": "string (base branch for merge)",
  "current_phase": "number | \"done\"",
  "required_roles": {
    "backend": "boolean",
    "frontend": "boolean",
    "ux": "boolean",
    "devops": "boolean",
    "security": "boolean"
  },
  "api_spec_required": "boolean",
  "workspace": {
    "type": "\"monorepo\" | \"multi-project\"",
    "projects": ["string (project paths)"]
  },
  "phases": {
    "T": { "status": "pending | in_progress | completed | skipped" },
    "0": { "status": "..." },
    "1": { "status": "..." },
    "2": { "status": "..." },
    "3": { "status": "..." },
    "4": { "status": "..." },
    "4.5": { "status": "..." },
    "5": { "status": "..." },
    "6": { "status": "..." }
  },
  "last_checkpoint": {
    "timestamp": "ISO 8601 string",
    "phase": "number",
    "summary": "string"
  },
  "meeting_ref": "string (optional, meeting name)",
  "meeting_dir": "string (optional, path to meeting artifacts)"
}
```

### Field Reference

<StateFieldTable fields={[
  { field: "work_name", type: "string", description: "Identifier for the current workflow" },
  { field: "docs_dir", type: "string", description: "Path where artifacts are stored" },
  { field: "branch", type: "string", description: "Git branch created for this workflow" },
  { field: "base_branch", type: "string", description: "Branch to merge back into" },
  { field: "current_phase", type: "number | \"done\"", description: "Active phase number" },
  { field: "required_roles", type: "object", description: "Boolean flags for which agent roles are active" },
  { field: "api_spec_required", type: "boolean", description: "Whether API specification document is needed" },
  { field: "workspace", type: "object", description: "Project structure metadata" },
  { field: "phases", type: "object", description: "Per-phase status (pending / in_progress / completed / skipped)" },
  { field: "last_checkpoint", type: "object", description: "Timestamp and summary of last saved progress" },
  { field: "meeting_ref", type: "string?", description: "Reference to a linked meeting (when --meeting was used)" },
  { field: "meeting_dir", type: "string?", description: "Path to the linked meeting's artifacts" }
]} caption=".jarfis-state.json field reference" />

---

## Learning Files

### Global Learnings: `~/.claude/jarfis-learnings.md`

Located in the user's home directory. Shared across all projects. Contains two main sections:

- **Agent Hints** — Behavioral rules and guidelines accumulated from retrospectives
- **Workflow Patterns** — Proven patterns for common scenarios

This file is loaded during Phase 0 (Pre-flight) of every workflow.

### Project Context: `.jarfis/context.md`

Project-specific context injected into agent prompts. Contains tech stack details, conventions, domain knowledge, and integration constraints.

### Project Profile: `.jarfis/project-profile.md`

Auto-generated by `/jarfis:project-init`. Describes the project's structure, dependencies, build tools, and configuration. Updated via `/jarfis:project-update`.

---

## Phase Reference

<PhasePipeline phases={[
  { id: "T", name: "Triage", description: "Request classification (A/B/C type determination)" },
  { id: "0", name: "Pre-flight", description: "Git synchronization, branch creation, learning file loading" },
  { id: "1", name: "Discovery", description: "PO reverse-questions, Working Backwards, PRD, feasibility assessment" },
  { id: "2", name: "Architecture & Planning", description: "Impact analysis, system design, API spec (conditional), task breakdown, test strategy" },
  { id: "3", name: "UX Design", description: "Screen design, interaction design, SVG assets, design tokens (conditional: only when UI is needed)" },
  { id: "4", name: "Implementation", description: "BE/FE/DevOps parallel implementation (only parts with tasks)" },
  { id: "4.5", name: "Operational Readiness", description: "Deployment strategy, rollback plan, operational readiness check" },
  { id: "5", name: "Review & QA", description: "API contract verification, Tech Lead + QA + Security parallel reviews" },
  { id: "6", name: "Retrospective", description: "Learning accumulation (global learnings + project context)" }
]} />

## Gate Reference

<GateCard gate={{
  number: 1,
  position: "After Phase 1",
  options: [
    { label: "Approve", description: "Proceed to Phase 2" },
    { label: "Revise", description: "Request changes and re-present" },
    { label: "Abort", description: "Cancel workflow" }
  ]
}} />

<GateCard gate={{
  number: 2,
  position: "After Phases 2 & 3",
  options: [
    { label: "Approve", description: "Proceed to Phase 4" },
    { label: "Revise", description: "Request changes and re-present" },
    { label: "Abort", description: "Cancel workflow" }
  ]
}} />

<GateCard gate={{
  number: 3,
  position: "After Phase 5",
  options: [
    { label: "Approve", description: "Proceed to Phase 6" },
    { label: "Revise & re-review", description: "Request fixes and trigger another review cycle" },
    { label: "Abort", description: "Cancel workflow" },
    { label: "Revisit design", description: "Return to Phase 2 for fundamental redesign" }
  ]
}} />

---

## See Also

- [Quick Start](/en/docs/getting-started) — Install and run your first workflow
- [Architecture & Concepts](/en/docs/concepts-overview) — Deep dive into the orchestration model
- [Guides & Customization](/en/docs/guides-customization) — Advanced workflow patterns
